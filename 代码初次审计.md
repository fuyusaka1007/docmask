# DocMask 代码初次审计报告

> 审计日期：2026-08-04  
> 审计类型：首次静态代码审计  
> 审计范围：核心脱敏与恢复引擎、TXT/DOCX/DOC Handler、CLI、CustomTkinter UI、依赖与测试、README 与 HANDOFF 说明  
> 结论等级：**存在阻止发布的 P0 问题，建议修复并完成跨平台回归后再发布**

## 1. 审计结论摘要

DocMask 已具备相对完整的产品结构，但当前实现仍存在若干会直接影响脱敏正确性、可逆性和用户数据安全的问题。

最重要的结论如下：

1. macOS 触摸板滚动问题不能只归因于多个 `CTkScrollableFrame`。应用自身每 500ms 在 Tk 主线程执行同步 HTTP 调试请求，是更直接的周期性卡顿来源。
2. `.doc` 转换流程会在转换成功后再次创建空的临时 `.docx`，真正的转换结果没有被返回，导致 `.doc` 主流程可能处理空文件。
3. `.doc` 输出仍使用 `.doc` 扩展名，但内容由 `python-docx` 按 DOCX/OOXML 格式写入，文件扩展名和真实格式不一致。
4. DOCX 超链接处理会统计替换次数，但没有将替换后的文字写回文档，可能出现“界面显示脱敏成功，实际仍包含敏感信息”的情况。
5. 脚注和尾注的 OPC relationship 查找方式不正确，相关内容很可能从未被处理。
6. `run.text = ...` 的写回策略可能删除 Run 内的换行、Tab、域代码或绘图等非文本节点，无法满足“复杂文档格式稳定保留”的承诺。
7. CLI 的 `--output` 对文件和目录的语义没有区分，批量处理可能失败，显式输出路径还可能覆盖现有文件。
8. 精确规则与正则规则之间缺少完整的 replacement 冲突校验，不能保证恢复结果正确。
9. UI 中覆盖率报告、帮助按钮、日志功能、拖放提示、默认设置等多处存在“界面已提供但功能未真正生效”的情况。
10. 当前 85 项测试没有覆盖 `.doc` 实际转换、超链接写回、脚注、复杂 Run、CLI 批量输出和 macOS 触摸板滚动等关键路径。

## 2. 审计方法与限制

本次审计采用以下方式：

- 阅读 `HANDOFF.md`、`README.md`、UI 调试文档和设计文档。
- 静态检查 `docmask/core/`、`docmask/handlers/`、`docmask/ui/`、`docmask/services/`、CLI 和配置代码。
- 对照测试用例确认已有覆盖范围和缺失场景。
- 对照 CustomTkinter 6.0.0 当前滚动容器实现及 python-docx 官方 API。
- 使用 Python 3.12 对源码和测试执行 `compileall`，语法检查通过。

限制：当前审计环境未安装项目所需的 `pytest`、`customtkinter`、`python-docx` 等完整运行依赖，因此未在本环境独立复跑测试套件，也无法直接复现 macOS 触摸板事件。本报告中与 macOS 手势有关的结论应通过真实 macOS 设备进一步验证。

## 3. 风险等级定义

| 等级 | 含义 |
|---|---|
| P0 | 可能导致未脱敏内容泄露、文件损坏、错误恢复、主流程不可用或明显卡顿，发布前必须修复 |
| P1 | 功能与文案不一致、稳定性不足、错误反馈缺失或重要体验问题，应在下一轮集中修复 |
| P2 | 性能、可维护性、可测试性或边界体验问题，可在 P0/P1 完成后处理 |

## 4. P0 问题

### P0-1：Tk 主线程存在周期性同步 HTTP 调试请求

**位置**：

- `docmask/ui/app.py:9-13`
- `docmask/ui/app.py:91-93`
- `docmask/ui/app.py:150-152`

**现象**：

`_debug_heartbeat()` 每 500ms 调用 `_debug_event()`，后者通过 `urllib.request.urlopen()` 同步访问 `http://127.0.0.1:7777/event`，超时设置为 0.2 秒。

**风险**：

- 同步调用发生在 Tk 主线程，最坏情况下每 500ms 阻塞约 200ms。
- 容易表现为触摸板滚动抖动、动画断续、点击延迟或窗口短暂无响应。
- `workbench_page.py` 和 `codebook_page.py` 反向导入 `app._debug_event`，形成不必要的循环导入风险。

**修复建议**：

1. 删除 `_debug_event()`、`_debug_heartbeat()`、`_debug_layout_snapshot()` 以及所有调用。
2. 删除 `workbench_page.py`、`codebook_page.py` 中对 `docmask.ui.app._debug_event` 的导入。
3. 如仍需诊断，改用内存环形缓冲；退出时一次性写入文件，禁止在 Tk 主线程同步访问网络。
4. 删除 `.dbg/` 和已完成的临时调试会话文件，或将其移至开发工具目录。

### P0-2：macOS 滚动根因尚未被充分证明

**位置**：

- `docmask/ui/pages/workbench_page.py:78`
- `docmask/ui/pages/codebook_page.py:55`
- `docmask/ui/pages/settings_page.py:43`
- `docmask/ui/pages/results_page.py:189`
- `HANDOFF.md` 的 macOS 滚动问题章节

**分析**：

CustomTkinter 的 `CTkScrollableFrame` 确实会为每个实例注册全局 `<MouseWheel>` 事件，但当前实现会沿 `event.widget` 的父级链检查事件是否属于该实例。普通兄弟页面不会天然同时通过该检查。因此，HANDOFF 中“多个 page 级实例必然同时滚动并互相冲突”的结论证据不足。

官方实现参考：

- <https://raw.githubusercontent.com/TomSchimansky/CustomTkinter/master/customtkinter/windows/widgets/ctk_scrollable_frame.py>

**修复建议**：

1. 首先删除 P0-1 的同步调试心跳，然后重新测试。
2. 建立只有一个滚动容器的最小 macOS 复现程序。
3. 记录以下字段，但不要进行同步网络上报：`event.delta`、事件控件、目标 Canvas、滚动前后 `yview`、scrollregion、控件是否 mapped。
4. 如果单实例仍抖动，新增项目自有的 `PageScrollFrame`：
   - 应用级只保留一个滚轮路由器；
   - 按控件祖先链选择唯一且当前可见的 Canvas；
   - macOS 使用较小的滚动增量；
   - 顶部或底部继续向外滚动时直接消费事件；
   - 销毁容器时从注册表移除，不使用会影响其它组件的 `unbind_all()`。
5. 不再采用方向锁、惯性窗口或粗暴去抖，这些策略会破坏触摸板自然惯性。

**验收标准**：

- 在真实 macOS 设备连续双指滚动 60 秒无反向跳动。
- 同方向 delta 下 `yview` 保持单调变化。
- 滚动到顶部和底部后画面稳定，不在两个位置间反复变化。
- 四个页面往返切换 20 次后滚动仍正常。
- 鼠标滚轮、触摸板、滚动条拖动均可用。

### P0-3：`.doc` 转换成功后返回了新的空临时文件

**位置**：`docmask/handlers/doc_handler.py:50-62`

**现象**：

`_try_pywin32_convert()` 和 `_try_libreoffice_convert()` 内部已经创建并写入临时 DOCX，但只返回布尔值。调用方发现返回 `True` 后，又调用 `_temp_docx_path()` 创建一个新的空文件并返回。

**风险**：

- 后续 `DocxHandler` 读取的是空文件或无效 ZIP。
- `.doc` 脱敏和恢复主流程不可依赖。
- 现有测试仅检查 Handler 类型，没有执行真实转换，因此未发现该问题。

**修复建议**：

- 两个转换方法改为返回 `Path | None`，直接返回实际转换文件。
- `_convert_to_docx()` 不再创建第二个临时文件。
- 为 Word 和 LibreOffice 分别增加真实集成测试。
- 没有转换器时在文件加入队列阶段就给出可理解的能力提示。

### P0-4：`.doc` 输出扩展名与实际格式不一致

**位置**：

- `docmask/ui/controller.py:193-200`
- `docmask/handlers/doc_handler.py:172-176`
- `docmask/handlers/docx_handler.py:110-114`

**现象**：

控制器根据原 `.doc` 输入生成 `.doc` 输出路径，随后 `DocxHandler` 使用 `python-docx` 保存。得到的实际内容是 DOCX/OOXML ZIP，文件名却仍是 `.doc`。

**修复建议**：

- 推荐第一版明确将 `.doc` 输入的结果输出为 `.docx`。
- 如果产品必须输出 `.doc`，则处理完成后必须再使用 Word/LibreOffice 转换回旧版 DOC。
- 在 README 和 UI 中明确输出格式策略。
- 保存后检查文件签名和扩展名是否一致。

### P0-5：`.doc` 临时目录不能被正确清理

**位置**：`docmask/handlers/doc_handler.py:28-36, 107-112`

**现象**：

LibreOffice 临时目录被加入 `_temp_files`，统一清理时却调用 `os.remove()`。目录删除会失败，异常被静默忽略。

**修复建议**：

- 使用 `tempfile.TemporaryDirectory()` 管理目录。
- 或根据路径类型分别调用 `unlink()` 和 `shutil.rmtree()`。
- 不要维护进程级全局 `_temp_files`；每个任务只清理自己创建的资源。

### P0-6：Word COM 异常时可能遗留 WINWORD 进程

**位置**：`docmask/handlers/doc_handler.py:82-105`

**问题**：

只有 `pythoncom.CoUninitialize()` 位于 `finally` 中。如果 `Documents.Open()` 或 `SaveAs2()` 抛出异常，`doc.Close()` 和 `word.Quit()` 不一定执行。

**修复建议**：

- 初始化 `word = None`、`doc = None`。
- 使用嵌套 `try/finally`，分别保证文档关闭、Word 退出和 COM 释放。
- 打开文档时关闭宏和交互式提示，避免后台任务挂起。

### P0-7：超链接显示文本没有写回，但替换次数被计入成功结果

**位置**：

- `docmask/handlers/docx_handler.py:425-438`
- `docmask/handlers/docx_handler.py:577-586`

**现象**：

代码调用 `masker.mask_text(hyperlink.text)` 或 `restorer.restore_text(hyperlink.text)`，但没有修改 `hyperlink.runs` 或底层 `w:t`。同时，替换次数仍被累计。

**风险**：

这是隐私工具中的高风险静默失败：报告显示已替换，真实文档仍保留敏感文字。

**修复建议**：

- 遍历 `hyperlink.runs`，使用与普通 Run 相同的安全文本节点写回算法。
- 在输出文件保存后重新打开并断言敏感原文不再存在。
- 添加跨多个 hyperlink run 的替换与恢复测试。

python-docx 官方 API 说明：

- <https://python-docx.readthedocs.io/en/latest/api/text.html>

### P0-8：脚注和尾注 relationship 查找方式错误

**位置**：

- `docmask/handlers/docx_handler.py:388-420`
- `docmask/handlers/docx_handler.py:554-575`

**问题**：

`doc.part.related_parts` 通常按 relationship ID 索引，当前代码使用 relationship type URI 直接 `.get()`，很可能始终得到 `None`。

**修复建议**：

- 遍历 `doc.part.rels.values()`，按 `rel.reltype` 匹配 footnotes/endnotes，然后取得 `rel.target_part`。
- 或直接扫描 OPC package 中对应 part。
- 不允许捕获异常后只记录 warning 并继续报告整体成功；对声明支持的内容应失败或明确标记“部分处理”。

### P0-9：Run 写回可能破坏文档内容和格式

**位置**：`docmask/handlers/docx_handler.py:258-279`

**问题**：

长度发生变化时，代码将整段替换文本写入第一个有内容的 Run，并对其余 Run 执行 `run.text = ""`。赋值 `Run.text` 会重建该 Run 的内容，可能删除：

- `w:tab`
- `w:br`
- 域代码和指令文本
- 绘图或其它非文本子节点
- 原有的分段格式边界

**修复建议**：

1. 直接定位所有 `w:t` 文本节点，不对整个 Run 的 `.text` 赋值。
2. 根据原始文本位置建立字符到 XML 文本节点的映射。
3. 只修改受替换范围影响的 `w:t` 内容，保留所有非文本节点。
4. 对跨 Run 且 replacement 长度变化的情况定义明确的格式继承策略。
5. 增加包含加粗切换、Tab、换行、域、图片和超链接的 golden-file 测试。

### P0-10：DOCX 支持范围与文档承诺不一致

当前实现没有完整覆盖以下内容：

- 页眉页脚中的表格和文本框
- 嵌套表格
- 批注与批注回复
- 修订记录中的插入/删除文字
- 扩展属性和自定义属性
- SmartArt、图表、嵌入对象和 OLE 文档
- 超链接 URL 本身
- 部分文本框或 DrawingML 文本

**修复建议**：

- 建立统一的 OPC part 能力矩阵。
- 对支持的 part 执行脱敏；对发现但不支持的含文本 part 给出明确警告。
- 保存后执行二次残留扫描；对支持范围内仍命中原文的文档采用 fail-closed。
- README 不再使用“覆盖全元素”等绝对描述。

### P0-11：CLI `--output` 文件/目录语义错误且可能覆盖文件

**位置**：

- `docmask/cli.py:106-108`
- `docmask/cli.py:176-178`
- `docmask/cli.py:225, 241`

**问题**：

- `args.output` 被原样作为完整文件路径传给 Handler。
- 批量任务传入输出目录时，目录可能被当作文件保存。
- 多文件任务会共享同一个输出值。
- 显式输出文件已存在时可能被直接覆盖，与 README 的“不覆盖”承诺不一致。

**修复建议**：

增加统一的 `resolve_output_path()`：

- 单文件输入：允许完整文件路径或目录。
- 批量输入：`--output` 必须为目录。
- 无论自动还是显式路径，都默认不覆盖现有文件。
- 使用同目录临时文件写入，校验通过后再原子替换或重命名。

### P0-12：规则校验不能完整保证可逆性

**位置**：

- `docmask/core/codebook.py:97-103`
- `docmask/core/codebook.py:130-175`
- `docmask/core/masker.py:103-111`

**问题**：

1. 同一原文重复定义时静默采用最后一条规则。
2. 正则 replacement 与精确 replacement 之间没有统一去重。
3. 正则 replacement 可能等于某条精确 replacement，恢复时会被错误还原成其它原文。
4. `pattern.subn(replacement, result)` 会解释 replacement 中的 `\1`、`\g<name>` 等组引用。如果密码本 replacement 被定义为普通文本，这会造成异常或非预期替换。
5. 未拒绝可以匹配空字符串的正则，可能产生大规模插入。
6. 精确规则执行后，正则会继续处理已生成的 replacement；多个正则也会顺序处理前一规则的输出，因此并非真正的一次遍历。

**修复建议**：

- 重复原文改为 ERROR，而不是 last-wins。
- 对全部精确和正则 replacement 做统一唯一性检查。
- replacement 按字面值写回时使用 `pattern.subn(lambda _: replacement, text)`。
- 拒绝空匹配正则。
- 建立跨阶段冲突检测，或重构为基于原始文本区间的统一单遍替换。
- 把“结构校验通过”和“对当前文档可逆”区分为两个检查阶段。

## 5. P1 问题

### P1-1：后台线程直接调用 Tk 方法

**位置**：`docmask/ui/controller.py:257-260`

后台线程会调用 `winfo_exists()` 和 `after()`。Tkinter 的线程行为取决于 Tcl/Tk 构建方式，窗口关闭期间尤其容易产生竞态。

建议后台线程只向 `queue.Queue` 写入纯数据事件，Tk 主线程通过固定 `after()` 周期消费队列。窗口关闭时先停止轮询、请求取消并等待一个有上限的退出周期。

### P1-2：任务控制器缺少顶层 `try/finally`

**位置**：`docmask/ui/controller.py:145-255`

Engine 构造、回调调度或循环外代码一旦异常，`task_running` 可能永久保持 `True`。

建议整个 worker 使用顶层 `try/except/finally`，并确保最终状态更新和完成事件只发送一次。

### P1-3：覆盖率报告开关没有实际作用

**位置**：

- `docmask/ui/state.py:94`
- `docmask/ui/pages/workbench_page.py:502-503`
- `docmask/ui/controller.py:213-223`

`generate_report` 会被 UI 修改，但 Controller 和 Handler 从不依据它决定是否生成或保留报告。

建议：关闭时不保存逐规则命中明细；开启时在结果页提供隐私安全的摘要，默认只显示规则编号，不显示原文映射。

### P1-4：自定义输出目录缺少校验

**位置**：`docmask/ui/pages/workbench_page.py:487-500`

用户选择“指定目录”但未填写路径时，`output_dir` 会被设为 `None`，最终悄悄输出到原目录。

建议将有效输出目录加入 `can_execute` 条件，并在输入旁显示明确错误。

### P1-5：添加大目录会阻塞 UI

**位置**：`docmask/ui/controller.py:88-101`

目录在 UI 主线程中被递归扫描两次：一次收集支持文件，一次统计全部文件。大型目录、网络磁盘或权限受限目录会造成明显卡顿。

建议单次遍历并放入后台任务，提供扫描进度和取消能力，逐项处理权限错误。

### P1-6：结果页空状态不会重置统计卡片

**位置**：`docmask/ui/pages/results_page.py:207-243`

当文件列表变空时只显示空状态，没有把总文件、成功、失败和替换总数重置为 0。

### P1-7：设置与帮助页面存在空实现

**位置**：`docmask/ui/pages/settings_page.py:481-489`

- 帮助按钮无响应。
- `on_show()` 不同步当前状态。
- 设置不会持久化。
- 页面初始显示“深色”，而 App 初始主题设置为“跟随系统”。
- 在设置页修改格式、输出模式和报告开关后，已创建的工作台控件不会完整同步。

建议实现统一的 Settings model、持久化文件和页面间状态通知。

### P1-8：UI 声称支持拖放，但没有实现

**位置**：`docmask/ui/widgets/file_queue.py:153-159`

空状态显示“拖放文件或文件夹到这里”，但代码没有任何拖放事件绑定。应实现拖放，或在实现前修改文案为“点击按钮添加”。

### P1-9：`--verify` 参数未实现

**位置**：`docmask/cli.py:249-250`

参数已暴露给用户，但 `cmd_restore()` 完全没有读取它。建议立即从 CLI 隐藏，或定义原文输入、哈希/结构比较和失败退出码后再开放。

### P1-10：UI 日志功能没有初始化对应日志系统

**位置**：

- `docmask/ui/__init__.py`
- `docmask/ui/app.py`
- `docmask/ui/pages/settings_page.py:446-479`

CLI 会调用 `setup_logging()`，UI 启动流程没有调用。设置页的打开和清空日志可能只是操作当前工作目录中的旧文件。

建议将日志目录迁移到平台用户数据目录，并在 UI 启动时显式初始化。macOS 应使用 Application Support/Logs，Windows 使用 LocalAppData。

## 6. P2 问题

### P2-1：Aho-Corasick 实际没有启用

**位置**：`docmask/core/masker.py:47-58`

`_build_automaton()` 已定义但从未调用，因此 `AC_AUTOMATON_THRESHOLD` 和 README 中“超过 100 条自动启用”的描述不成立。

建议在修复前删除该依赖和宣传，或实现真正的线性扫描算法并补充大规则集性能基准。当前 `_mask_with_automaton()` 若直接启用，还需要先检查其逐位置迭代是否会退化为高复杂度。

### P2-2：依赖与 Python 版本声明不一致

**位置**：

- `requirements.txt`
- `setup.py:9-14`

问题包括：

- `setup.py` 没有声明 UI 依赖。
- `setup.py` 与 `requirements.txt` 内容不同。
- 代码使用 `Paragraph.hyperlinks`，但依赖允许安装不提供该 API 的旧版 `python-docx`。
- `python_requires>=3.8` 与部分模块直接使用的 `list[...]` 注解不一致。

建议：

- 统一采用 `pyproject.toml` 管理依赖。
- Python 最低版本设为 3.9 或 3.10。
- 根据实际 API 锁定 `python-docx` 最低版本，并设置合理上限或兼容测试。
- 将 CLI、UI、DOC 转换和开发测试依赖拆分为 extras。

### P2-3：文件收集效率和重复项处理

**位置**：

- `docmask/services/file_service.py:47-54`
- `docmask/ui/controller.py:75-86`

`collect_files()` 会为每种格式重复遍历目录。`add_files()` 的 `existing_paths` 在本次循环中不更新，同一个路径在一次调用中重复出现时可能被加入多次。

建议单次目录遍历、使用规范化绝对路径去重，并明确符号链接策略。

### P2-4：TXT 输出编码被固定改为 UTF-8

**位置**：`docmask/handlers/txt_handler.py:23-33`

输入会检测编码，但输出始终使用 UTF-8。该行为未在文档中说明。

建议允许“保留原编码”或明确统一转换为 UTF-8，并为 BOM、GBK、无效字节和换行符保留增加测试。

### P2-5：文件写入不是原子的

TXT 和 DOCX 都直接写入最终输出路径。进程崩溃、磁盘空间不足或保存异常时可能留下不完整文件。

建议写入同目录临时文件，完成格式校验后用 `os.replace()` 原子落盘。

## 7. 测试缺口

仓库中存在 85 个测试函数，但需要新增以下测试：

### 7.1 DOC 转换

- Word 转换成功时返回真实文件路径。
- LibreOffice 转换成功时返回真实文件路径。
- 转换失败时不残留进程和临时目录。
- `.doc` 输入输出为合法 `.docx`，或完成正确的 DOC 回转。
- mask→restore 往返测试。

### 7.2 DOCX 结构

- 超链接单 Run、多 Run、跨格式 Run。
- 脚注和尾注。
- 正文与页眉页脚文本框。
- 页眉页脚表格、嵌套表格。
- Tab、换行、域代码、图片和绘图同 Run。
- 长度增加与缩短的跨 Run 替换。
- linked header/footer 去重处理。
- 保存后残留原文扫描。

### 7.3 Codebook 和 Masker

- 重复原文定义。
- 精确与正则 replacement 重复。
- replacement 含反斜杠和组引用字符。
- 空匹配正则。
- 正则匹配前序 replacement 的跨阶段冲突。
- replacement 互为前缀或包含关系。

### 7.4 CLI 和文件输出

- 单文件输出到目录。
- 单文件输出到指定文件。
- 批量输出到目录。
- 显式输出已存在时不覆盖。
- 同名输入文件的自动编号。
- `--verify` 行为或参数不存在性。

### 7.5 UI 与线程

- worker 异常后 `task_running` 必须复位。
- 窗口关闭期间取消任务不会调用已销毁的 Tk。
- 设置页面与工作台状态同步。
- 覆盖率报告开关真正生效。
- 清空结果后统计归零。
- 大目录扫描可取消。

### 7.6 跨平台手工/自动验收矩阵

| 平台 | 鼠标滚轮 | 触摸板 | 主题切换 | 缩放 | DOC 转换 | 打包启动 |
|---|---:|---:|---:|---:|---:|---:|
| macOS Intel | 必测 | 必测 | 必测 | 必测 | LibreOffice | 必测 |
| macOS Apple Silicon | 必测 | 必测 | 必测 | 必测 | LibreOffice/Word | 必测 |
| Windows 10/11 | 必测 | 可选 | 必测 | 必测 | Word/LibreOffice | 必测 |
| Linux | 必测 | 可选 | 必测 | 必测 | LibreOffice | 必测 |

## 8. 建议修复顺序

### 第一阶段：恢复可信主流程

1. 删除所有 UI 调试 HTTP 插桩。
2. 修复 `.doc` 转换路径、输出格式、资源清理和 COM 生命周期。
3. 修复超链接与脚注/尾注写回。
4. 重构 DOCX 文本节点写回，避免破坏非文本节点。
5. 增加输出后残留扫描和原子保存。
6. 修复规则唯一性和正则 replacement 语义。

### 第二阶段：修复跨平台 UI

1. 在 macOS 重新复现滚动问题。
2. 必要时引入统一滚轮路由和自有 `PageScrollFrame`。
3. 后台线程改为 Queue→Tk 主线程事件模型。
4. 大目录扫描移至后台。
5. 修复设置同步、结果统计、日志和无效按钮。

### 第三阶段：统一 CLI、依赖和文档

1. 统一 CLI 输出路径解析。
2. 删除或实现 `--verify`。
3. 使用 `pyproject.toml` 统一依赖和入口点。
4. 更新 README、HANDOFF 和 DOCX 能力矩阵。
5. 建立 Windows/macOS/Linux CI 与真实应用打包 smoke test。

## 9. 文档修订建议

应从 README 和 HANDOFF 中删除或修正以下当前不准确的表述：

- “核心引擎和所有 Handler 已完成、稳定”。
- “DOC 完整支持”。
- “DOCX 覆盖全元素”。
- “每处文本只替换一次，replacement 不再参与匹配”。
- “精确规则超过 100 条自动启用 AC 自动机”。
- “拖放文件或文件夹到这里”。
- “`--output` 支持文件或目录”但没有明确单文件/批量语义。
- “设置与帮助完整实现”。

建议新增一张用户可见的能力表：

| 内容类型 | 当前状态 | 目标状态 |
|---|---|---|
| TXT 正文 | 支持 | 支持并说明编码策略 |
| DOCX 普通正文 | 部分支持 | 安全保留 XML 非文本节点 |
| DOCX 表格 | 部分支持 | 支持嵌套表格 |
| DOCX 页眉页脚 | 部分支持 | 包含表格和文本框 |
| DOCX 超链接文字 | 当前写回失效 | 完整支持并测试 |
| DOCX 脚注尾注 | 当前关系查找可疑 | 完整支持并测试 |
| DOCX 批注/修订 | 不支持 | 明确警告或实现 |
| DOC | 当前主流程存在缺陷 | 输出 DOCX 或完成双向转换 |
| 正则规则恢复 | 不可逆 | 继续明确标注不可逆 |

## 10. 发布门槛建议

满足以下条件后再生成正式安装包：

1. 所有 P0 已修复并有回归测试。
2. `.doc` 和复杂 DOCX 测试使用真实生成文件验证，不只 mock。
3. 输出文件保存后能够重新打开并通过残留扫描。
4. macOS 触摸板验收通过。
5. Windows 和 macOS 打包产物在干净机器上完成 smoke test。
6. README、HANDOFF 和实际功能一致。
7. 测试日志、错误信息和覆盖率报告默认不暴露密码本映射或文档正文。

## 11. 本次审计未执行的操作

- 未修改现有业务代码。
- 未删除调试目录或测试数据。
- 未安装新的项目依赖。
- 未重新生成 Windows/macOS 打包产物。
- 未声称当前 85 项测试已在本审计环境通过。

本报告应作为首次审计基线。完成修复后，建议另行生成“复审报告”，逐条记录问题状态、对应提交、测试证据和剩余风险。
