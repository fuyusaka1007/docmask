# P0-10 至 P1-6 修复与测试报告

> 测试日期：2026-08-04  
> 修复范围：P0-10、P0-11、P0-12、P1-1～P1-6  
> 当前环境：Windows，Microsoft Word，LibreOffice 26.2.5.2  
> 最终结论：**代码回归和 Windows 双转换器真实 `.doc` 往返测试通过；macOS 未在本轮实机复验。**

## 1. 结论摘要

- 最终自动化测试：**107 passed**。
- Python 语法/字节码检查：`compileall` 通过。
- Windows + Microsoft Word：真实 `.doc → .docx → mask → restore` 通过，替换 **51 / 51**。
- Windows + LibreOffice：真实 `.doc → .docx → mask → restore` 通过，替换 **38 / 38**。
- 两条链路的恢复可见文本均与各自转换稿完全一致。
- 原始 `.doc` 的 SHA-256、大小、修改时间均未改变。
- 测试结束后新增 `WINWORD/soffice/soffice.bin` 进程：**0**。
- 测试结束后新增 `docmask_doc_*`、`docmask_lo_profile_*` 临时目录：**0**。

## 2. 修复项与验证结果

| 审计项 | 修复内容 | 主要验证 | 结果 |
|---|---|---|---|
| P0-10 | 建立 DOCX OPC 能力矩阵；支持递归表格、批注正文/作者、修订插入/删除文本、核心文本属性、有限扩展/自定义字符串属性；保存后对支持部件做残留扫描；不支持部件只告警 | 嵌套表格+修订 round-trip、批注 round-trip、真实 Word/LibreOffice 文稿、能力矩阵断言 | 通过 |
| P0-11 | 单文件输出可为文件或目录；批处理输出强制为目录；重名自动递增；同目录临时写入并以不覆盖方式提交 | 输出解析、已有文件保护、全量 Handler 回归 | 通过 |
| P0-12 | 重复原文报错；空匹配正则报错；脱敏词全局唯一；精确/正则统一从原文坐标选取；正则替换按字面值写入；结构校验与文档可逆性分离 | 重复/空匹配/跨类型重复、`\1` 字面替换、生成文本不二次处理、正则不可逆提示 | 通过 |
| P1-1 | 工作线程只写 `queue.Queue`；仅主线程调用 `after()`；关闭时停止轮询并有界等待线程 | 线程所有者断言、Controller 执行测试 | 通过 |
| P1-2 | Worker 顶层 `try/except/finally`；初始化失败也复位状态并只调用一次完成回调 | 强制引擎初始化失败 | 通过 |
| P1-3 | 报告关闭时 `coverage=None`；开启时仅保留匿名规则 ID、类型、次数 | 开/关双分支与敏感词不出现在报告断言 | 通过 |
| P1-4 | 自定义输出目录有效性纳入 `can_execute`；工作台先同步路径再校验 | AppState 前置条件测试 | 通过 |
| P1-5 | 目录改为后台单遍扫描；支持进度、取消和逐项访问错误 | 单遍结果、跳过计数、进度回调 | 通过 |
| P1-6 | 结果页刷新时先将四张统计卡归零 | 空状态伪 UI 回归测试 | 通过 |

## 3. DOCX 能力边界

### 3.1 强保证范围

- `word/document.xml`
- `word/header*.xml`、`word/footer*.xml`
- `word/footnotes.xml`、`word/endnotes.xml`
- `word/comments*.xml` 中的正文、作者与 initials
- `w:t` 与修订删除文本 `w:delText`
- `docProps/core.xml` 的文本语义字段
- `docProps/app.xml` 的 Company、Manager、Template
- `docProps/custom.xml` 的字符串值

这些部件在保存后会重新打开并扫描；仍命中规则原文时不提交目标文件。

### 3.2 告警范围

- Charts、SmartArt/Diagram
- Embedded/OLE 对象
- 外部超链接 URL
- 其他未声明支持的 XML 文本部件

这些内容不会被静默宣称为“已覆盖”。若检测到疑似规则原文，记录在 `DocxHandler.last_warnings`，需要人工复核。

## 4. 自动化测试

最终命令：

```powershell
$env:PYTHONPATH='E:\project\docmask\docmask\.test_runtime'
python -m pytest -q
python -m compileall -q docmask
```

最终结果：

```text
........................................................................ [ 67%]
..................................                                       [100%]
107 passed
```

新增重点测试文件：`tests/test_audit_p0_p1_regressions.py`。Controller 的线程与报告测试同时扩充在 `tests/test_ui_controller.py`。

## 5. 真实旧版 DOC 集成测试

### 5.1 输入与隔离

- 原文：`C:\Users\asus\xwechat_files\wxid_x51nldwtavgs22_b5eb\msg\file\2026-08\测试文档.doc`
- 文件大小：33,792 字节
- 测试前 SHA-256：`CF539C9E7951B8ED13BAC93B0356CC53F09838A12D12071B6C0DF41CEA1E608E`
- 测试后 SHA-256：与测试前完全相同
- 最终隔离目录：`test-artifacts/real_doc_p0_10_p1_6_20260804_v3/`

测试先复制原文，再生成只含精确规则的隔离密码本。所有 converted/masked/restored 文稿均写入隔离目录，未修改外部原文或项目中原有 Word 文档。

### 5.2 Microsoft Word

| 检查 | 结果 |
|---|---|
| 转换 DOCX 可读 | 通过 |
| mask DOCX 可读 | 通过 |
| restore DOCX 可读 | 通过 |
| mask / restore 次数 | 51 / 51 |
| mask 后文本发生变化 | 通过 |
| restore 文本等于转换稿 | 通过 |
| 返回后新增 WINWORD 进程 | 0 |

Word 使用 `DispatchEx` 独立实例，禁用宏与交互提示。退出时释放文档、Application、COM 引用并等待该实例 PID；超时只定向终止本任务创建的实例，不影响用户已有 Word 进程。

### 5.3 LibreOffice

| 检查 | 结果 |
|---|---|
| 转换 DOCX 可读 | 通过 |
| mask DOCX 可读 | 通过 |
| restore DOCX 可读 | 通过 |
| mask / restore 次数 | 38 / 38 |
| mask 后文本发生变化 | 通过 |
| restore 文本等于转换稿 | 通过 |
| 返回后新增 soffice 进程 | 0 |

LibreOffice 使用独立临时 `UserInstallation` profile，并启用 `--headless --nologo --nodefault --nofirststartwizard --nolockcheck --norestore`。转换超时上限调整为 120 秒，临时 profile 离开上下文后删除。

原始机器可读结果：`test-artifacts/real_doc_p0_10_p1_6_20260804_v3/real-doc-integration-result.json`。

## 6. 测试中发现并纠正的问题

1. 首轮 Word 转换稿的 core 属性包含日期和 revision 数字。残留扫描错误地把这些强类型字段当作普通文本，产生误报。最终仅扫描实际支持改写的 core 文本字段。
2. LibreOffice 首轮首次启动超过 60 秒。补齐无界面/无恢复启动参数并将上限调整为 120 秒后稳定通过。
3. 第二轮 Word 功能往返通过，但独立实例退出超过测试脚本 15 秒观察窗。最终增加 PID 级有界等待与定向兜底，第三轮返回时残留为 0。

这些失败轮次没有修改源文档，且轮次结束时全局资源复查均无新增残留。

## 7. 尚未覆盖与发布建议

- 本轮运行环境是 Windows，**未在 macOS 实机重跑 LibreOffice**；跨平台代码路径已保留，但发布前仍需 macOS 实机验证。
- 本轮真实文档验证以 OOXML 有效性和可见文本往返为主，未重新做逐页视觉渲染对比。既有报告已记录 LibreOffice 转换旧 DOC 时可能丢失水印，因此高保真 Windows 转换仍建议优先使用 Microsoft Word。
- 图表、SmartArt、OLE、外部 URL 属于告警范围，不应把无告警之外的复杂对象解释为自动脱敏承诺。

## 8. 最终判定

P0-10～P0-12、P1-1～P1-6 已按推荐方向完成代码修复，并通过自动化回归与 Windows 双转换器真实文档集成测试。当前可判定 **Windows 功能验收通过**；macOS + LibreOffice 与复杂对象人工复核属于发布前补充验证项。
