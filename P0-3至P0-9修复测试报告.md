# DocMask P0-3 至 P0-9 修复测试报告

> 测试日期：2026-08-04  
> 修复范围：首次审计报告中的 P0-3、P0-4、P0-5、P0-6、P0-7、P0-8、P0-9  
> 最终结论：**代码回归、真实旧版 `.doc` 的 Word/LibreOffice 转换及 mask→restore 往返均通过；LibreOffice 转换存在水印丢失的视觉保真问题，macOS 尚未实机复验。**

## 1. 修复结果

### P0-3：转换成功后返回空临时文件

状态：**已修复**。

- Word 和 LibreOffice 转换方法不再返回布尔值，而是返回转换器实际生成的 `Path`。
- `_convert_to_docx()` 直接返回真实转换结果，不再创建第二个空 DOCX。
- 转换结果增加 ZIP/OOXML 基础有效性检查。

主要文件：`docmask/handlers/doc_handler.py`

### P0-4：`.doc` 扩展名与 DOCX 内容不一致

状态：**已修复**。

- `.doc` 输入经过处理后统一输出 `.docx`。
- UI Controller 生成 `.doc` 任务输出路径时显式指定 `.docx` 扩展名。
- `generate_output_path()` 新增 `output_extension` 参数。
- 即使调用方传入 `result.doc`，`DocHandler` 也会规范化为 `result.docx`。

主要文件：

- `docmask/handlers/doc_handler.py`
- `docmask/ui/controller.py`
- `docmask/utils/file_utils.py`

### P0-5：临时目录无法清理

状态：**已修复**。

- 删除进程级 `_temp_files` 和 `atexit` 清理机制。
- 每次 `.doc` 任务使用独立的 `TemporaryDirectory`。
- 无论成功、失败或取消，离开任务上下文后临时目录都会被清理。

### P0-6：Word COM 异常时遗留资源

状态：**已修复**。

- 使用 `DispatchEx` 创建独立 Word 实例。
- `Document.Close()`、`Word.Quit()` 和 `CoUninitialize()` 分别位于受保护的清理流程中。
- 转换失败时仍保证全部生命周期动作执行。
- 禁用 Word 可见窗口和交互式提示。

### P0-7：超链接只计数、不写回

状态：**已修复**。

- 改为直接遍历 `w:hyperlink` 下的 `w:r/w:t`。
- 脱敏和恢复都会真实修改超链接显示文本。
- 正文、表格、文本框以及现有页眉页脚 part 中的超链接均可被处理。
- 集成测试验证超链接由“张三”变为“匿名客户001”，恢复后重新变为“张三”。

主要文件：`docmask/handlers/docx_handler.py`

### P0-8：脚注/尾注 relationship 查找错误

状态：**已修复**。

- 不再用 relationship type URI 作为 `related_parts` 字典键。
- 改为遍历 `doc.part.rels.values()`，按 `RT.FOOTNOTES` 和 `RT.ENDNOTES` 匹配 `target_part`。
- 脚注/尾注段落查询由直接子节点改为递归查找。
- 脚注/尾注处理失败时抛出任务错误，不再仅记录 warning 后继续报告成功。

### P0-9：Run 写回破坏非文本节点

状态：**已修复**。

- 不再对整个 `Run.text` 赋值或清空 Run。
- 新算法只修改 `w:t` 文本节点。
- 使用文本差异映射把长度变化后的结果分配回原文本节点。
- 遇到 Tab、换行、域、绘图等非文本节点时切断匹配组，禁止跨越这些节点替换。
- 正确保留 `xml:space="preserve"`。
- 页眉页脚改为只遍历包中已经存在的 relationship，避免保存时创建额外空 part。

## 2. 新增测试

新增测试文件：

- `tests/test_doc_handler.py`
- `tests/test_docx_complex_integration.py`
- `tests/word_fixture_factory.py`
- `tests/generate_p0_word_artifacts.py`

覆盖内容：

- 转换器返回真实输出路径。
- `.doc` 任务输出 `.docx`。
- DOCX ZIP 有效性。
- 临时目录自动清理。
- Word COM 保存失败时仍执行 Close、Quit 和 CoUninitialize。
- 跨 Run 且 replacement 长度变化。
- 超链接脱敏与恢复。
- 脚注和尾注脱敏与恢复。
- Tab、换行、绘图、域代码和域指令保留。
- 页眉和表格文本处理。
- Word package part 数量不发生意外增长。

## 3. 自动化测试结果

### 3.1 语法检查

命令：

```text
python -m compileall -q docmask tests
```

结果：**通过**。

### 3.2 全量 pytest

命令：

```text
python -m pytest -q
```

结果：

```text
91 passed in 2.95s
```

修复前仓库有 85 项测试，本轮新增 6 项集成和回归测试。

## 4. 隔离 Word 集成测试

最终测试工作区：

```text
test-artifacts/p0_3_to_p0_9_20260804_v4/
```

生成的测试文稿：

| 文件 | 用途 |
|---|---|
| `input/complex_source.docx` | 新建的复杂源文稿 |
| `output/complex_masked.docx` | 脱敏结果 |
| `output/complex_restored.docx` | 恢复结果 |
| `config/codebook.txt` | 隔离测试密码本 |
| `integration-result.json` | 机器可读测试结果 |

测试源文稿包含：

- 跨两个格式 Run 的“张三”。
- 两个 Run 组成的超链接显示文本“张三”。
- 域显示文本及完整 fldChar/instrText 结构。
- 一个绘图节点及其后的敏感文本。
- 表格敏感文本。
- 页眉敏感文本。
- 真实 footnotes.xml 和 endnotes.xml part。
- Tab 和换行节点。

### 4.1 往返结果

| 指标 | 源文稿 | 脱敏文稿 | 恢复文稿 |
|---|---:|---:|---:|
| 原文“张三”可见文本命中 | 8 | 0 | 8 |
| replacement“匿名客户001”命中 | 0 | 8 | 0 |
| Handler 替换计数 | — | 8 | 8 |
| DOCX ZIP 有效 | 是 | 是 | 是 |
| `word/*.xml` part 数 | 11 | 11 | 11 |

### 4.2 结构保留结果

以下节点在源文稿、脱敏文稿和恢复文稿中数量完全一致：

| 节点 | 数量 |
|---|---:|
| `w:tab` | 1 |
| `w:br` | 1 |
| `w:drawing` | 1 |
| `w:fldChar` | 3 |
| `w:instrText` | 1 |
| `w:hyperlink` | 1 |

### 4.3 集成检查

以下检查全部通过：

1. 根目录现有 Word 文件未变化。
2. 源文稿包含待脱敏原文。
3. 脱敏文稿不再包含原文。
4. 脱敏文稿包含 replacement。
5. 恢复文稿不再包含 replacement。
6. 恢复后的原文命中数与源文稿一致。
7. 脱敏前后非文本结构一致。
8. 恢复前后非文本结构一致。
9. 脱敏后 Word XML part 数量不变。
10. 恢复后 Word XML part 数量不变。
11. 三个 DOCX 均为有效 ZIP。
12. 超链接真实完成脱敏。
13. 超链接真实完成恢复。
14. 脱敏和恢复替换计数相等。

机器结果：`test-artifacts/p0_3_to_p0_9_20260804_v4/integration-result.json`

## 5. 现有 Word 文档隔离结果

集成测试只读取新建的隔离文稿。项目根目录下以下 6 个原有 Word 文件未被作为输入，测试前后的文件大小和修改时间完全一致：

- `22zt鑴辨晱_desensitized.docx`
- `22zt鑴辨晱_desensitized_restored.docx`
- `input_origin.docx`
- `input_origin_2.docx`
- `output.docx`
- `output_restored.docx`

## 6. 渲染检查

已从 The Document Foundation 官方站点安装并验证 LibreOffice 26.2.5.2，安装包数字签名状态为 `Valid`，签名者为 The Document Foundation。实际命令位于：

```text
C:\Program Files\LibreOffice\program\soffice.com
```

安装程序没有把 `soffice` 自动加入 PATH，且系统 PATH 的 UAC 更新被取消。为避免依赖系统 PATH，`DocHandler` 已增加标准安装目录探测：先查 PATH，再查 Windows、macOS 和 Linux 的常见 LibreOffice 路径；Windows 优先使用适合命令行捕获输出的 `soffice.com`。

隔离文稿重新生成在 `test-artifacts/p0_3_to_p0_9_20260804_v4/`。源文稿、脱敏文稿、恢复文稿均通过真实 LibreOffice 转换为 PDF：

| PDF | 大小（字节） | 转换结果 |
|---|---:|---|
| `render/libreoffice/complex_source.pdf` | 69,475 | 通过 |
| `render/libreoffice/complex_masked.pdf` | 77,593 | 通过 |
| `render/libreoffice/complex_restored.pdf` | 69,412 | 通过 |

三次转换退出码均为 0，转换结束后的残留 `soffice` 进程数为 0。测试夹具原有的无效 1×1 PNG 数据已替换为通过 Pillow 校验的 PNG，重新转换时不再出现 `libpng` 解码错误。

本轮验证了真实 LibreOffice 排版/导出链路能够完成，但**尚未声明人工视觉对比通过**；正式发布前仍建议逐页比较三份 PDF 的分页、脚注位置、图片布局和字体替换。

## 7. 真实旧版 DOC 外部集成

### 7.1 输入与隔离

使用用户提供的只读旧版文稿 `测试文档.doc`。文件头为 OLE Compound File 标识 `D0 CF 11 E0 A1 B1 1A E1`，不是改扩展名的 DOCX。

| 指标 | 结果 |
|---|---|
| 文件大小 | 33,792 字节 |
| 测试前 SHA-256 | `CF539C9E7951B8ED13BAC93B0356CC53F09838A12D12071B6C0DF41CEA1E608E` |
| 测试后 SHA-256 | 与测试前完全相同 |
| 源文件大小/修改时间 | 完全不变 |
| 隔离副本 | 哈希与源文件相同 |

最终工作区：`test-artifacts/real_doc_20260804_v3/`。

项目现有密码本的多个脱敏词本来就已出现在该文稿中，程序首次测试时正确抛出 `MaskConflictError`，避免不可逆恢复。最终测试保留原密码本的 26 条精确匹配原词，生成短且唯一的测试专用替换词；不可逆正则规则未用于往返测试。项目原密码本未修改。

### 7.2 Windows + Microsoft Word

通过 `pywin32` 的独立 `Word.Application` 实例完成真实 `.doc` → `.docx`，再交给 `DocxHandler` 执行 mask → restore。

| 检查 | 结果 |
|---|---|
| 转换 DOCX 有效 | 通过 |
| 脱敏 DOCX 有效 | 通过 |
| 恢复 DOCX 有效 | 通过 |
| 脱敏/恢复次数 | 51 / 51 |
| 恢复文本与转换稿完全相同 | 通过 |
| 转换稿与恢复稿 PNG 哈希 | 两页均完全相同 |
| 水印“AI 生成” | 保留 |
| 新增残留 WINWORD 进程 | 0 |

### 7.3 Windows + LibreOffice 26.2.5.2

使用独立临时 `UserInstallation` profile 完成真实 `.doc` → `.docx`，再执行 mask → restore。profile 位于系统临时目录，转换结束后由 `TemporaryDirectory` 自动删除。

| 检查 | 结果 |
|---|---|
| 转换 DOCX 有效 | 通过 |
| 脱敏 DOCX 有效 | 通过 |
| 恢复 DOCX 有效 | 通过 |
| 脱敏/恢复次数 | 38 / 38 |
| 恢复文本与转换稿完全相同 | 通过 |
| 转换稿与恢复稿 PNG 哈希 | 两页均完全相同 |
| 新增残留 soffice 进程 | 0 |

Word 与 LibreOffice 生成的 DOCX 组成不同，因此替换计数不同；两条链路都以各自的转换稿为基线实现了精确恢复。

### 7.4 逐页视觉检查

Word 和 LibreOffice 各自的转换稿、脱敏稿、恢复稿共 6 份 DOCX 均渲染为 2 页 PNG/PDF，12 张 PNG 已按原始分辨率逐页检查：

- 未发现文字裁切、重叠、乱码、标题错位或异常分页。
- 脱敏稿因测试占位符比原数字长，发生预期内的换行和分页重排，但没有内容越界。
- Word 的转换稿与恢复稿逐页完全一致，并保留右下角“AI 生成”水印。
- LibreOffice 的转换稿与恢复稿逐页完全一致，但在旧 DOC → DOCX 阶段丢失右下角“AI 生成”水印；正文、标题和分页保持正常。

因此 Windows LibreOffice 链路的**功能往返通过，视觉保真有条件通过**。如果水印属于必须保留的业务内容，应优先使用 Microsoft Word 转换，或为 LibreOffice 增加水印结构补偿；macOS + LibreOffice 仍需实机复验。

### 7.5 最终资源清理

最终复查结果：

- `WINWORD`、测试创建的 `soffice/soffice.bin` 残留进程：0。
- `docmask_doc_*`、`docmask_lo_profile_*`、`soffice_profile_*`、`soffice_convert_*` 测试临时目录：0。
- 测试期间人工中断的两次渲染留下 4 个空临时目录，已按精确路径清理并复查为 0。

## 8. 最终结论

P0-3 至 P0-9 的代码缺陷已按推荐方向完成修复，91 项自动化测试全部通过，隔离 DOCX 集成测试的 14 项检查全部通过，真实旧版 `.doc` 的 Word 和 LibreOffice 功能往返均通过，源文档未被修改。

当前剩余事项是 LibreOffice 旧 DOC 转换的水印保真处理，以及 macOS + LibreOffice 实机复验。Windows 上建议将 Microsoft Word 作为高保真旧 DOC 转换的首选后端。
