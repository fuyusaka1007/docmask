# DocMask UI 原型与布局设计

> 技术方案：Python + CustomTkinter  
> 产品定位：纯本地、离线、可逆的文档脱敏与恢复工具  
> 支持格式：TXT / DOCX / DOC

## 1. 设计目标

1. 让首次使用者在一个主流程内完成“选择模式 → 选择密码本 → 添加文件 → 风险预检 → 执行 → 查看结果”。
2. 把密码本错误、正则不可逆、脱敏词与原文冲突等高风险信息放在执行前，而不是藏在日志中。
3. 支持单文件和目录批处理，并清晰展示每个文件的状态、替换次数、输出位置及失败原因。
4. 保持纯本地工具的可信感：明确展示“离线运行、文件不上传”，日志中不暴露敏感原文或映射内容。
5. UI 与现有核心模块解耦，界面层只负责参数收集、任务调度、状态展示和错误转换。

## 2. 总体信息架构

采用“单主窗口 + 左侧导航 + 右侧内容区”的桌面结构，不使用多个独立主窗口。

| 页面 | 作用 | 导航名称 |
|---|---|---|
| 工作台 | 创建并执行脱敏/恢复任务 | 工作台 |
| 密码本 | 加载、校验和查看密码本概况 | 密码本 |
| 任务结果 | 查看本次及当前会话的处理结果 | 任务结果 |
| 设置与帮助 | 管理外观、日志、默认格式及查看说明 | 设置与帮助 |

辅助弹窗：

- 冲突详情弹窗
- 执行确认弹窗
- 密码本校验详情弹窗
- 文件详情弹窗
- 关于弹窗

## 3. 主窗口框架

### 3.1 推荐尺寸

- 默认窗口：`1180 × 760 px`
- 最小窗口：`980 × 640 px`
- 左侧导航宽度：`220 px`
- 顶部标题区高度：`72 px`
- 内容区外边距：`24 px`
- 卡片间距：`16 px`
- 圆角：卡片 `12 px`，按钮/输入框 `8 px`

窗口缩放策略：

- 左侧导航固定宽度，右侧内容区自适应拉伸。
- 宽度小于 `1040 px` 时隐藏导航底部说明文字，仅保留核心导航。
- 高度不足时，页面主体使用 `CTkScrollableFrame`，底部执行栏保持可见。
- 不建议使用移动端式布局，目标是 Windows/macOS/Linux 桌面环境。

### 3.2 全局布局线框

```text
┌──────────────────────────────────────────────────────────────────────────┐
│  DocMask                                                        — □ ×  │
├──────────────────┬───────────────────────────────────────────────────────┤
│                  │  页面标题                              状态/辅助操作  │
│  ◈ DocMask       ├───────────────────────────────────────────────────────┤
│  本地文档保护     │                                                       │
│                  │                                                       │
│  ● 工作台        │                   当前页面内容                         │
│  ○ 密码本        │                                                       │
│  ○ 任务结果      │                                                       │
│  ○ 设置与帮助    │                                                       │
│                  │                                                       │
│                  │                                                       │
│  ─────────────   │                                                       │
│  ● 纯本地运行    │                                                       │
│  文件不会上传     │                                                       │
│  v1.x.x          │                                                       │
└──────────────────┴───────────────────────────────────────────────────────┘
```

### 3.3 导航状态

- 当前页面：主色浅底 + 左侧 `3 px` 主色指示条 + 高亮图标和文字。
- 普通页面：透明底，悬停时使用次级背景。
- “任务结果”存在失败任务时显示红色圆点；存在已完成结果时可显示数量徽标。
- 执行任务期间仍可进入任务结果页，但禁止重复启动新任务。

## 4. 页面一：工作台

### 4.1 页面目标

完成脱敏和恢复的核心流程。默认打开此页，并记忆用户上一次选择的模式与输出位置偏好，但不自动记忆密码本内的敏感内容。

### 4.2 页面布局

页面分为顶部模式切换、任务配置区、文件队列区和底部执行区。

```text
┌───────────────────────────────────────────────────────────────────────┐
│ 工作台                                              ● 就绪 / 处理中   │
│ 创建一个脱敏或恢复任务                                                 │
├───────────────────────────────────────────────────────────────────────┤
│ ┌──────────────── 模式切换 ─────────────────────────────────────────┐ │
│ │ [  脱敏文档  ]   [  恢复文档  ]                                   │ │
│ │ 脱敏：替换敏感信息，可使用同一密码本恢复                           │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 1. 密码本 ────────────────────────────────────────┐ │
│ │ [ codebook.txt                                      ] [选择文件] │ │
│ │ ✓ 校验通过   42 条精确规则 · 2 条正则规则        [查看详情]      │ │
│ │ ⚠ 正则规则不可逆，恢复时无法还原正则匹配的原始内容                 │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 2. 待处理文件 ────────────────────────────────────┐ │
│ │ [ + 添加文件 ] [ + 添加文件夹 ]  格式：[✓DOCX][✓DOC][✓TXT]       │ │
│ │ ┌───────────────────────────────────────────────────────────────┐ │ │
│ │ │ 文件名              格式     大小       状态           操作   │ │ │
│ │ │ 合同.docx           DOCX     1.8 MB     等待预检       移除   │ │ │
│ │ │ 客户名单.txt        TXT      42 KB      等待预检       移除   │ │ │
│ │ └───────────────────────────────────────────────────────────────┘ │ │
│ │ 2 个文件 · 支持拖放文件或文件夹                                   │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 3. 输出设置 ──────────────────────────────────────┐ │
│ │ (●) 保存到原文件所在目录  ( ) 指定目录                            │ │
│ │ [ D:\安全输出                                      ] [浏览]      │ │
│ │ 文件名自动添加：_desensitized；重名时自动追加序号，不覆盖原文件   │ │
│ │ [✓] 生成覆盖率报告（仅脱敏）                                      │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌───────────────────────────────────────────────────────────────────┐ │
│ │ 2 个文件待处理  ·  密码本已通过校验      [清空] [预检并执行脱敏] │ │
│ └───────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────────┘
```

### 4.3 模式切换

使用分段按钮或两个大号选项卡：

- `脱敏文档`：主色为青蓝，按钮文案为“预检并执行脱敏”。
- `恢复文档`：主色可保持一致，增加“恢复”图标，按钮文案为“执行恢复”。
- 模式切换后保留密码本，但清空与模式不兼容的文件队列状态。
- 恢复模式隐藏“生成覆盖率报告”；暂不展示尚未实现的恢复校验开关。

### 4.4 密码本卡片状态

未选择：

- 虚线边框或中性输入框。
- 提示“请选择 `.txt` 密码本”。
- 主执行按钮禁用。

校验中：

- 显示小型进度动画和“正在读取并校验…”。

校验通过：

- 绿色状态点。
- 显示精确规则数、正则规则数。
- 若包含正则规则，额外显示黄色不可逆提示。

校验失败：

- 红色边框与错误摘要，如“发现 3 个错误”。
- 显示“查看详情”和“重新选择”。
- 主执行按钮禁用。

安全要求：

- 主界面不展示密码本中的原文与脱敏词。
- 详情弹窗默认仅展示规则行号、错误类型和脱敏词风险；确需显示映射时需用户主动点击“显示敏感内容”。

### 4.5 文件队列

推荐使用一个自定义表格容器，每行由 `CTkFrame` 组成，避免依赖 ttk Treeview 的主题不一致问题。

列定义：

| 列 | 内容 |
|---|---|
| 文件名 | 图标、文件名，悬停提示完整路径 |
| 格式 | DOCX / DOC / TXT 标签 |
| 大小 | 格式化文件大小 |
| 状态 | 等待预检 / 预检通过 / 冲突 / 处理中 / 完成 / 失败 |
| 操作 | 移除、查看详情、打开输出目录 |

空状态：

- 中间显示文档图标。
- 主文案“拖放文件或文件夹到这里”。
- 次文案“支持 TXT、DOCX、DOC，可批量处理”。
- 提供“选择文件”按钮。

目录导入：

- 默认递归扫描子目录，与当前 CLI 行为一致。
- 导入完成后显示“已发现 N 个支持的文件，跳过 M 个其他格式文件”。
- 格式筛选器允许用户取消某种格式。

### 4.6 输出设置

- 默认保存到原文件所在目录。
- 指定目录时启用路径输入框与“浏览”按钮。
- 明确提示不会覆盖原文件，已有同名结果会自动追加序号。
- 脱敏后缀：`_desensitized`。
- 恢复后缀：`_restored`。
- 第一版不开放自定义后缀，避免增加不必要配置。

### 4.7 执行前预检

点击“预检并执行脱敏”后先对所有文件进行文档级冲突预检。

预检通过：

- 直接进入执行状态。
- 文件行状态从“预检通过”变为“处理中”。

存在冲突：

- 不处理冲突文件。
- 多文件任务中，弹窗允许用户选择“仅执行通过预检的文件”或“返回修改密码本”。
- 单文件任务中默认阻止执行，不提供强制继续按钮，因为继续会破坏可逆性。

冲突弹窗线框：

```text
┌────────────────────── 发现脱敏词冲突 ──────────────────────┐
│  !  1 个文件无法安全脱敏                                  │
│                                                           │
│  合同.docx                                                │
│  · 脱敏词 “%” 已在原文出现 3 次                           │
│  · 脱敏词 “!” 已在原文出现 1 次                           │
│                                                           │
│  继续处理会导致恢复时无法区分原始字符和脱敏结果。          │
│  建议将常见符号替换为专用占位符，例如 ⟦DM-D6⟧。          │
│                                                           │
│  [查看密码本指南]             [返回修改密码本] [执行安全文件]│
└───────────────────────────────────────────────────────────┘
```

### 4.8 执行中状态

- 禁用模式切换、密码本选择、文件增删和再次执行。
- 底部执行栏改为总进度：`正在处理 2 / 8`。
- 使用确定进度条展示文件级进度；单个 DOCX 内部没有可靠细粒度回调时，不伪造百分比。
- 当前文件行使用不确定进度动画。
- 提供“停止任务”按钮；停止后不删除已生成的成功文件。
- 后台任务使用线程或任务队列，严禁在 Tk 主线程直接处理大文件。

## 5. 页面二：密码本

### 5.1 页面目标

集中管理本次使用的密码本，并以非敏感方式解释其可逆性风险。第一版不内置完整编辑器，避免在 UI 中长期暴露原始敏感映射。

### 5.2 页面布局

```text
┌───────────────────────────────────────────────────────────────────────┐
│ 密码本                                             [选择密码本文件]  │
│ 校验规则完整性，提前发现不可逆风险                                    │
├───────────────────────────────────────────────────────────────────────┤
│ ┌──────────────── 当前密码本 ───────────────────────────────────────┐ │
│ │ codebook.txt                                     ✓ 校验通过       │ │
│ │ D:\DocMask\codebook.txt                                           │ │
│ │ 42 条精确规则       2 条正则规则       UTF-8       刚刚校验       │ │
│ │ [重新校验]  [在系统中定位]  [更换文件]                            │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────┐ ┌──────────────┐ ┌────────────────────────────────┐ │
│ │ 可逆规则     │ │ 正则规则     │ │ 风险检查                       │ │
│ │ 42           │ │ 2            │ │ 1 项警告                       │ │
│ │ 精确匹配     │ │ 不可逆       │ │ 建议处理后再执行               │ │
│ └──────────────┘ └──────────────┘ └────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 校验结果 ─────────────────────────────────────────┐ │
│ │ ✓ 未发现重复脱敏词                                                │ │
│ │ ✓ 未发现精确规则交叉冲突                                          │ │
│ │ ✓ 正则表达式格式有效                                              │ │
│ │ ⚠ 包含 2 条正则规则，匹配到的原始内容无法恢复                     │ │
│ │ ℹ 文档级脱敏词冲突将在添加待处理文件后检查                        │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 安全提示 ─────────────────────────────────────────┐ │
│ │ 密码本包含原文与脱敏词的对应关系，请妥善保管。DocMask 不上传文件。 │ │
│ └───────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────────┘
```

### 5.3 校验结果分级

- `错误`：红色，阻止执行，例如脱敏词重复、交叉冲突、格式错误、空规则。
- `警告`：黄色，允许执行但需明确风险，例如正则规则不可逆、原文与脱敏词相同。
- `提示`：蓝灰色，例如文档级冲突需在选择文件后检查。
- `通过`：绿色，仅表示密码本结构校验通过，不代表对任意文档都无冲突。

### 5.4 密码本指南区

页面底部可折叠展示：

- 格式示例：`原文==>脱敏词`。
- 正则示例及“不可恢复”说明。
- 占位符建议：使用不易自然出现的专用标记，例如 `⟦DM-NAME-01⟧`。
- 明确不建议使用 `%`、`!`、`￥` 等常见单字符符号作为数字替换值。

## 6. 页面三：任务结果

### 6.1 页面目标

展示当前会话中的任务状态与输出，不读取或长期保存敏感处理历史。应用重启后可清空会话结果，仅保留普通日志文件。

### 6.2 页面布局

```text
┌───────────────────────────────────────────────────────────────────────┐
│ 任务结果                                           [清空会话记录]    │
│ 查看处理结果、失败原因与覆盖率                                      │
├───────────────────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────────────┐ │
│ │ 总文件 8 │ │ 成功 6   │ │ 失败 2   │ │ 替换总数 1,284          │ │
│ └──────────┘ └──────────┘ └──────────┘ └──────────────────────────┘ │
│                                                                       │
│ [全部] [成功] [冲突] [失败]                      [搜索文件名……]      │
│ ┌───────────────────────────────────────────────────────────────────┐ │
│ │ 文件名       操作   状态       替换数   输出文件        操作      │ │
│ │ 合同.docx    脱敏   ✓ 完成     128      合同_des…      打开目录  │ │
│ │ 名单.txt     脱敏   ! 冲突     —        —              查看原因  │ │
│ │ 归档.doc     恢复   × 失败     0        —              查看原因  │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 选中文件详情 ─────────────────────────────────────┐ │
│ │ 状态：冲突                                                        │ │
│ │ 原因：文档中已自然存在密码本脱敏词，已阻止执行。                  │ │
│ │ 建议：修改密码本中的冲突脱敏词后重试。                            │ │
│ │                                                [重新添加到工作台] │ │
│ └───────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────────┘
```

### 6.3 结果状态

| 状态 | 颜色 | 操作 |
|---|---|---|
| 完成 | 绿色 | 打开文件、打开所在目录、查看覆盖率 |
| 冲突 | 橙红色 | 查看冲突、返回密码本、重新添加 |
| 失败 | 红色 | 查看错误、重试、打开日志 |
| 已停止 | 灰色 | 重新添加到工作台 |
| 跳过 | 灰蓝色 | 查看跳过原因 |

### 6.4 覆盖率详情

仅脱敏任务且用户启用报告时展示：

- 总替换次数。
- 命中规则数量与未命中规则数量。
- 默认不展示原文规则内容，按“规则 #12”标识。
- 用户主动确认后才可显示敏感规则内容。
- 支持复制摘要，但复制前提示内容可能包含敏感信息。

## 7. 页面四：设置与帮助

### 7.1 页面布局

```text
┌───────────────────────────────────────────────────────────────────────┐
│ 设置与帮助                                                           │
├───────────────────────────────────────────────────────────────────────┤
│ ┌──────────────── 外观 ─────────────────────────────────────────────┐ │
│ │ 主题                 [跟随系统 ▼]                                 │ │
│ │ 界面缩放             [100% ▼]                                     │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 处理默认值 ───────────────────────────────────────┐ │
│ │ 默认格式             [✓ DOCX] [✓ DOC] [✓ TXT]                     │ │
│ │ 默认输出位置         (●) 原文件目录  ( ) 上次使用目录             │ │
│ │ [✓] 脱敏任务默认生成覆盖率报告                                    │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 日志与隐私 ───────────────────────────────────────┐ │
│ │ 日志级别             [INFO ▼]                                     │ │
│ │ 日志仅记录状态与次数，不记录原文和密码本映射。                    │ │
│ │ [打开日志文件] [打开日志目录] [清空日志]                          │ │
│ └───────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│ ┌──────────────── 帮助 ─────────────────────────────────────────────┐ │
│ │ [密码本编写指南] [常见问题] [支持的文档范围] [关于 DocMask]      │ │
│ │ DOC 文件依赖 Microsoft Word 或 LibreOffice；复杂文本框可能受限。  │ │
│ └───────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────────┘
```

### 7.2 第一版设置范围

建议第一版只实现：

- 外观模式：跟随系统 / 浅色 / 深色。
- 界面缩放：90% / 100% / 110% / 125%。
- 默认文件格式。
- 默认输出位置策略。
- 默认覆盖率报告开关。
- 日志级别：INFO / DEBUG / WARNING / ERROR。

不建议第一版加入复杂账户、云同步、历史数据库或在线更新功能，以保持纯本地产品定位。

## 8. 视觉规范

### 8.1 风格方向

关键词：可信、安全、克制、清晰、现代桌面工具。

采用低饱和蓝灰色背景和青蓝主色，避免使用“黑客终端”风格或高饱和霓虹色。大面积内容使用卡片分组，通过状态色而不是装饰色表达风险。

### 8.2 建议色板

浅色模式：

| 用途 | 色值 |
|---|---|
| 页面背景 | `#F4F7FA` |
| 导航背景 | `#0F1B2D` |
| 卡片背景 | `#FFFFFF` |
| 主色 | `#168AAD` |
| 主色悬停 | `#11728F` |
| 主文字 | `#172033` |
| 次文字 | `#667085` |
| 边框 | `#DCE3EA` |
| 成功 | `#168A5B` |
| 警告 | `#D68B16` |
| 冲突/错误 | `#D94A4A` |
| 信息 | `#3B82C4` |

深色模式：

| 用途 | 色值 |
|---|---|
| 页面背景 | `#0F1722` |
| 导航背景 | `#09111E` |
| 卡片背景 | `#172230` |
| 主色 | `#38A6C7` |
| 主文字 | `#F1F5F9` |
| 次文字 | `#9AA8B8` |
| 边框 | `#2B3949` |

### 8.3 字体与字号

- Windows 中文优先：`Microsoft YaHei UI`。
- macOS 回退：`PingFang SC`。
- Linux 回退：`Noto Sans CJK SC`。
- 页面标题：`24 px / Semibold`。
- 卡片标题：`16 px / Semibold`。
- 正文：`14 px / Regular`。
- 辅助文本：`12 px / Regular`。
- 按钮：`14 px / Medium`。
- 数字统计：`24–28 px / Semibold`。

### 8.4 控件尺寸

- 主按钮高度：`42 px`。
- 次按钮高度：`36 px`。
- 输入框高度：`40 px`。
- 导航项高度：`44 px`。
- 表格行高：`48 px`。
- 标签高度：`24 px`。
- 可点击区域最小尺寸：`36 × 36 px`。

## 9. 组件清单与 CustomTkinter 映射

| 设计组件 | 建议实现 |
|---|---|
| 主窗口 | `CTk` |
| 页面容器 | `CTkFrame` |
| 可滚动页面 | `CTkScrollableFrame` |
| 导航按钮 | `CTkButton`，左对齐文本，自定义选中态 |
| 路径输入 | `CTkEntry` + `CTkButton` |
| 模式切换 | `CTkSegmentedButton` |
| 单选项 | `CTkRadioButton` |
| 复选项 | `CTkCheckBox` |
| 进度条 | `CTkProgressBar` |
| 日志/详情文本 | `CTkTextbox`，只读 |
| 下拉选择 | `CTkOptionMenu` |
| 状态标签 | 自定义 `CTkFrame` + `CTkLabel` |
| 文件表格 | `CTkScrollableFrame` 内自定义行组件 |
| 弹窗 | `CTkToplevel`，设置 transient 与 grab_set |
| 文件选择 | `tkinter.filedialog` |
| 消息确认 | 自定义 `CTkToplevel`，避免原生 MessageBox 风格割裂 |

图标建议使用本地 PNG 资源或 Unicode 简单符号；不得运行时联网加载。所有图标提供文字标签，不以颜色或图标作为唯一信息载体。

## 10. 关键交互流程

### 10.1 脱敏流程

1. 用户进入工作台，默认选择“脱敏文档”。
2. 选择密码本，系统立即执行加载与结构校验。
3. 用户添加一个或多个文件/文件夹。
4. 用户确认格式过滤与输出位置。
5. 点击“预检并执行脱敏”。
6. 系统逐文件执行文档级脱敏词冲突预检。
7. 无冲突文件进入处理；冲突文件被阻止并显示原因。
8. 页面显示总进度和逐文件状态。
9. 完成后自动进入任务结果页，显示成功、冲突、失败和替换总数。

### 10.2 恢复流程

1. 用户切换到“恢复文档”。
2. 选择执行脱敏时使用的同一密码本。
3. 添加已脱敏文件。
4. 选择输出位置。
5. 点击“执行恢复”。
6. 系统逐文件恢复并记录替换次数。
7. 完成后展示输出文件位置和失败原因。
8. 因 `--verify` 当前尚未实现，第一版 UI 不提供“与原文对比校验”。

### 10.3 密码本错误流程

1. 加载密码本失败时，密码本卡片转为错误状态。
2. 以用户可理解的语言展示错误类别和行号。
3. 主执行按钮保持禁用。
4. 用户可重新选择、在系统编辑器中打开，或查看编写指南。

### 10.4 DOC 依赖失败流程

- 检测到缺少 Microsoft Word / LibreOffice 时，在文件行展示“缺少 DOC 转换组件”。
- 提供建议：“安装 Microsoft Word/LibreOffice，或将文件另存为 DOCX 后重试”。
- 其他 DOCX/TXT 文件仍可继续处理。

## 11. 空状态、加载状态与异常状态

### 11.1 空状态

- 无密码本：突出“选择密码本”。
- 无文件：显示拖放区域和支持格式。
- 无任务结果：显示“完成一次任务后，结果会显示在这里”。
- 无日志：显示“当前没有日志记录”。

### 11.2 加载状态

- 密码本校验使用行内不确定进度条。
- 目录扫描使用“正在扫描文件…”状态，可取消。
- 文档处理使用文件级确定进度 + 当前文件不确定进度。

### 11.3 错误文案原则

错误文案结构固定为：

- 发生了什么。
- 为什么不能继续。
- 用户下一步可以做什么。

示例：

> 文档中已存在脱敏词 `%`，继续处理会导致恢复错误。请修改密码本中的该脱敏词后重试，建议使用 `⟦DM-D6⟧` 这类专用占位符。

禁止只显示 Python 异常堆栈。技术详情可折叠展示，并提供“复制技术信息”。

## 12. 可访问性与易用性

- 所有状态同时使用图标、文字和颜色表达。
- 支持键盘 Tab 顺序；Enter 触发主操作，Esc 关闭弹窗。
- 主要按钮具有明确焦点样式。
- 正文与背景对比度至少达到 WCAG AA。
- 路径过长时中间省略，悬停显示完整路径。
- 删除/清空等操作需要二次确认；移除尚未处理的单个文件可直接撤销提示。
- 任务执行期间关闭窗口需要确认：“任务仍在进行，关闭将停止处理”。

## 13. UI 代码结构建议

```text
docmask/ui/
├── __init__.py
├── app.py                  # 应用入口、主题和页面切换
├── controller.py           # 连接 UI 与 core/handlers，管理后台任务
├── state.py                # 当前模式、密码本、文件队列、任务结果状态
├── theme.py                # 色板、字号、间距、控件尺寸
├── pages/
│   ├── workbench_page.py   # 工作台
│   ├── codebook_page.py    # 密码本
│   ├── results_page.py     # 任务结果
│   └── settings_page.py    # 设置与帮助
├── widgets/
│   ├── sidebar.py
│   ├── path_picker.py
│   ├── file_queue.py
│   ├── status_badge.py
│   ├── summary_card.py
│   ├── alert_banner.py
│   └── dialogs.py
└── resources/
    └── icons/              # 本地图标资源
```

职责边界：

- UI 页面不得复制 `Masker`、`Restorer` 或 Handler 的业务逻辑。
- `controller.py` 负责创建 `Codebook`、调用校验、选择 Handler 并执行任务。
- 后台线程仅处理文件；所有控件更新通过 `after()` 回到 Tk 主线程。
- UI 内错误统一转换为用户文案，同时将技术详情写入安全日志。
- 不在日志中记录规则原文、脱敏词或文档正文。

## 14. 现有能力与 UI 对应关系

| UI 功能 | 当前代码能力 | 备注 |
|---|---|---|
| 密码本加载 | `Codebook.load()` | 已有 |
| 密码本结构校验 | `Codebook.validate()` | 已有 |
| 精确/正则规则数量 | `exact_rule_count` / `regex_rule_count` | 已有 |
| 文档脱敏 | `TxtHandler/DocxHandler/DocHandler.mask()` | 已有 |
| 文档恢复 | `restore()` | 已有 |
| 文档级冲突预检 | `Masker.precheck_conflict()`，由 Handler 调用 | 已有 |
| 批量文件收集 | CLI `_collect_files()` | 建议抽到公共服务供 UI 复用 |
| 输出自动命名 | `generate_output_path()` | 已有 |
| 覆盖率报告 | `Masker.generate_coverage_report()` | 已有 |
| 恢复后原文校验 | CLI 参数存在但未实现 | 第一版 UI 隐藏 |
| 执行进度回调 | 暂无统一接口 | UI 开发前建议增加文件级回调 |
| 任务取消 | 暂无统一接口 | 可先在文件间检查取消标记 |

## 15. 开发优先级

### P0：可用主流程

- 主窗口与侧边导航。
- 脱敏/恢复模式切换。
- 密码本选择与校验状态。
- 文件/目录选择与文件队列。
- 输出目录选择。
- 后台线程执行。
- 总进度、文件状态、成功/失败结果。
- 冲突预检弹窗。

### P1：体验完善

- 密码本独立页面。
- 任务结果筛选与文件详情。
- 覆盖率可视化摘要。
- 拖放文件支持。
- 主题、缩放与默认值设置。
- 打开结果文件/目录。

### P2：后续增强

- 安全的密码本编辑辅助。
- 恢复后与原文对比校验。
- 更细粒度进度回调。
- 会话报告导出。
- 选择性脱敏范围配置。

## 16. 第一版验收标准

1. 用户无需命令行即可完成 TXT/DOCX/DOC 的脱敏与恢复。
2. 密码本存在结构错误时不能启动任务，并能看到清晰原因。
3. 脱敏词与原文冲突时必须阻止对应文件执行，不允许强制绕过。
4. 正则规则存在时必须明确提示不可逆风险。
5. 多文件任务能展示每个文件的独立状态，不因单文件失败中断全部任务。
6. UI 处理文件时窗口保持响应，不出现主线程卡死。
7. 输出文件不会覆盖原文件，同名时自动增加序号。
8. 日志和界面默认不暴露密码本原文、脱敏词映射或文档正文。
9. 浅色与深色模式下文字、边框和状态均清晰可辨。
10. 在最小窗口尺寸下无控件重叠，关键执行按钮始终可访问。
